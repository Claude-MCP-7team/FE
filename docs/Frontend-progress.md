# Frontend 진행 기록

## 2026-09-19 · S-1 · S-2 확정 · FUTURE_PASS 연결

- **완료:** BE의 `future_eligible_from`과 `summary.future_eligible`을 읽어 네 번째 판정 분류를 연결했다. 매핑은 `ELIGIBLE`→PASS, `NEEDS_INFO`→UNKNOWN, `INELIGIBLE`+날짜→FUTURE_PASS, `INELIGIBLE`→FAIL이다. `confidence`는 매핑에 쓰지 않는다. 조건 수준 규칙과 같은 모양이라 두 층위가 같은 어법으로 읽힌다.
- **완료:** 집계 칸을 4칸으로 늘리면서 `future_eligible`을 `ineligible`에서 차감했다. 별도 분류가 아니라 부분집합이므로 그대로 더하면 합이 전체를 넘는다. 카드 배지와 집계 칸이 1:1로 맞는다.
- **완료:** 정책 배지 문구·기호를 `contracts.js`의 `statuses` 한 곳에서 가져온다. 실판정 배지에 기호가 빠져 있던 것도 함께 맞춰졌다. 날짜는 "예상 충족일"로 쓰고 접수 가능일로 표현하지 않는다.
- **검증:** `npm test` 94개, `npm run check` 통과. 검증기는 날짜가 `INELIGIBLE`이 아닌 판정에 붙거나, 달력에 없는 날이거나, 요약 건수와 어긋나거나, 부분집합이 `ineligible`을 넘으면 거부한다. 조건 하나가 FUTURE_PASS여도 정책이 승격되지 않는지, 네 칸의 합이 전체와 같은지 확인하는 테스트를 추가했다.
- **주의:** 기존 테스트의 `assert.equal('status' in view.results[1], false)`는 "FE가 정책 수준 status를 만들지 않는다"를 고정한 것이었다. 이제 BE 신호를 읽으므로 의미가 바뀌어, 조건에서 파생하지 않는다는 것을 확인하는 어서션으로 교체했다.
- **다음:** 실판정 Dashboard의 상태 필터, `needs_review_fields` 표시(필드명 사용자 문구 매핑 필요).
- **막힌 점:** 브라우저 확인 미완료. BE의 `title` 필드 대기.

## 2026-09-19 · 정책 제목 처리와 BE 스키마 확인

- **완료:** 카드·상세 제목의 `policy_id` 원문 노출을 고쳤다. `JudgementResult.title`이 오면 제목으로 쓰고(검증기가 문자열만 허용), 없으면 `code.policy-ref`로 식별자임을 드러낸다. ID를 제목 자리에 그대로 두면 정책 이름으로 읽히므로, 이름이 없다는 사실을 숨기지 않는 쪽을 택했다.
- **확인:** BE `dev`의 `app/schemas/judgement.py`에 정책명 필드가 없다. `GET /v1/policies/{id}`는 룰·근거 전문을 반환하고 `GET /v1/policies`는 limit 100·id 필터 없음(1,555건)이라 FE 단독 우회가 모두 부적절하다. BE가 판정 시점에 이미 `policy.title`을 쓰고 있으므로 `JudgementResult`에 한 필드 추가를 요청한다.
- **확인(중요):** 같은 스키마에 **`future_eligible_from`과 `summary.future_eligible`이 이미 있다.** 주석이 "마일스톤의 네 번째 판정 상태를 화면이 그릴 수 있는 단일 신호", "화면의 네 번째 배지가 쓰는 값"이라고 명시한다. S-2는 BE가 이미 해결해 두었고 FE가 읽지 않고 있을 뿐이다. `needs_review_fields`도 표시되지 않고 있다.
- **검증:** `npm test` 89개, `npm run check` 통과. 제목 유무·공백·비문자열·이스케이프를 검증하는 테스트 3개 추가.
- **다음:** S-2를 `future_eligible_from` 기준으로 확정하고 네 번째 배지·집계·필터를 연결. S-1도 함께 정리된다.
- **막힌 점:** 브라우저 확인 미완료. BE의 `title` 필드 추가 대기.

## 2026-09-19 · 제외 정책 블록 스타일과 인용 분리

- **완료:** `.conflict`를 정의했다. 제외는 판정이 아니므로 상태색을 쓰지 않고, `--bg` 배경에 좌측 중성 굵은 선으로 구분한다. 제외 사유에 원문 인용문이 설명 문장 끝에 이어 붙어 있던 것을 별도 인용 블록으로 분리했다. 표시되지 않던 `excluded[].confidence`에 S-4의 중성 칩을 적용해 추정 충돌이 확정된 제외처럼 보이지 않게 했다. `<summary>`에 클릭 가능 표시를 넣고 `--bg` 토큰을 정의했다.
- **완료:** 신뢰도 칩을 `src/confidence.js`로 분리해 판정 화면과 조합 화면이 공유한다. 문구가 화면마다 갈라지지 않는다.
- **검증:** `npm test` 86개, `npm run check` 통과. 조합 렌더 HTML 테스트가 없어 3개를 새로 추가했다. 인용 분리, 신뢰도 칩, 상태색 미사용, 응답 텍스트 이스케이프를 검증한다.
- **다음:** S-1·S-2를 BE와 합의.
- **막힌 점:** 브라우저 확인은 여전히 미완료. 제외 블록이 `<details>` 안에서 여러 개 쌓일 때의 간격과 인용 블록의 이중 배경이 실제로 어떻게 보이는지 확인이 필요하다.

## 2026-09-19 · S-4 확정 · 신뢰도 중성 칩 적용

- **완료:** 신뢰도를 판정 상태의 배지 색으로 보여주던 구현을 중성 칩으로 교체. `.chip`은 배경 없이 테두리만 쓰므로 판정 상태 4색과 형태·색 모두 겹치지 않는다. enum 원문 대신 "추정 포함"/"담당부서 확인"을 쓰고, 중복이던 카드 안내 문구는 enum 반복을 빼고 다음 행동만 말하도록 정리. S-4와 함께 처리하기로 한 `.notice-inline`도 같은 중성 계열로 정의.
- **검증:** `npm test` 83개, `npm run check` 통과. Dashboard·상세가 상태색을 신뢰도에 쓰지 않고 enum을 노출하지 않는지 검증을 추가. 칩 글자 대비 7.02, 테두리 3.42.
- **다음:** S-1·S-2(정책 수준 매핑과 FUTURE_PASS)를 BE와 합의.
- **막힌 점:** 브라우저 실행 환경이 없어 칩과 배지가 실제로 구별되는지, 카드 안에서 줄바꿈이 어떻게 되는지 확인하지 못했다.
- **결정 필요:** `NEEDS_REVIEW` 문구를 "담당부서 확인"으로 둘지. `.conflict` 스타일은 아직 미정.

## 2026-09-19 · Design 규격 문서 착수

- **완료:** 판정 상태·근거 표현 규격(`Design-states.md`)과 디자인 시스템(`Design-system.md`) 작성. 코드의 색·타이포·형태 토큰을 역정리하고 명도 대비 15건 측정. 미달 4건(두 파일의 입력·보조 버튼 테두리)을 수정하고 장식용 `--line`과 기능용 `--line-strong`을 분리. 카드 테두리·구분선은 유지.
- **확인:** dev 병합 후 `judgement-page.js`가 신뢰도를 판정 상태의 배지 색(PASS 녹색 / UNKNOWN 노랑)으로 렌더링하며 enum 원문을 그대로 노출하고 있음을 확인. 서로 다른 두 축이 같은 색 언어를 공유해 `ELIGIBLE` + `ESTIMATED` 정책이 "신청 가능"과 "추가 확인"으로 동시에 읽힌다. S-4로 정리했고 결정 항목 중 우선순위가 가장 높다.
- **확인:** 정책 카드·상세 제목이 `policy_id` 원문이며, 새 화면이 쓰는 `.judgement-card` 등 5개 클래스에 CSS 규칙이 없다.
- **다음:** S-4(신뢰도 표시 분리)를 먼저 확정하고 S-1·S-2(정책 수준 매핑과 FUTURE_PASS)를 BE와 합의. 이후 미구현 화면인 서류·일정 시안.
- **막힌 점:** 브라우저 실행 환경이 없어 키보드·스크린리더·실기기 검증 미완료.
- **결정 필요:** S-1~S-7, D-1~D-3. 특히 D-1은 `styles.css`(녹색)와 `profile.css`(파랑/slate)가 서로 무관한 팔레트 두 벌이라 프로필 화면만 다른 제품처럼 보이는 문제다.

## 2026-09-19 · M3 조합 추천 연결

- **완료:** BE `POST /v1/combinations` 계약을 검증하고, 서버 모드 `#/combinations`에서 세션 프로필을 전송해 `conservative/maximal` 조합과 제외 정책의 충돌 근거를 표시하도록 연결했습니다. 금액 추정 여부와 원문 URL을 보존하며 FE에서 조합 점수나 중복수혜 규칙을 재계산하지 않습니다.
- **검증:** 조합 응답의 순위·금액·구성 정책·제외 사유·신뢰도·출처 URL을 검증하고, API HTTP/응답 오류 및 화면 로딩·취소·재시도 상태를 처리했습니다.
- **다음:** BE `POST /v1/plan` 계약에 맞춘 서류 체크리스트와 신청 일정 타임라인 연결.

## 2026-09-19 · M3 역질문·재판정 흐름 연결

- **완료:** 최신 dev에서 BE `/v1/questions` 계약을 반영. 필드별 질문 병합·`resolves` 표시·boolean/number/choice 입력·원문 근거 검증을 추가하고, 실제 결과의 `#/questions` 화면에서 답변 저장 → 기존 `answers` 보존 → `/v1/judge` 동기 재판정 → 결과 Dashboard 복귀 흐름을 연결.
- **검증:** `npm.cmd test` 77개, `npm.cmd run check`, `git diff --check` 통과. 질문 DTO 중복·타입·선택지·숫자 경계, 네트워크/HTTP/응답/시간 초과, 빈 답변 생략, 프로필 전송을 검증.
- **다음:** 실제 BE/CORS 브라우저 E2E, 모름 선택에 대한 정책별 안내, 조합·서류·일정 API 연결. M1 동의·소득 계약과 M3 전체 완료 조건은 남아 있음.

## 2026-09-19 · PR #12 리뷰 반영 · 분석 로딩·화면 이탈 취소

- **완료:** 분석 버튼 클릭 직후 로딩 문구와 disabled 상태를 즉시 렌더링. `judgement-runner.js`가 단일 요청과 AbortController를 소유하고, 분석 화면을 떠나면 요청을 취소한다. 늦게 도착한 성공·오류는 현재 화면에 반영하지 않으며 사용자가 분석 화면에 남아 있을 때만 오류를 표시한다.
- **검증:** `npm.cmd test` 73개, `npm.cmd run check`, `git diff --check` 통과. 중복 클릭 방지, 즉시 로딩, 취소 신호, 화면 이탈 후 늦은 응답 무시, 재시도 가능 상태를 검증.
- **PR 답변:** PR #12 리뷰에서 지적한 로딩 피드백 누락과 화면 전환 중 백그라운드 요청 문제를 수정해 같은 PR에 추가 커밋으로 반영한다.

## 2026-09-19 · M2 분석 화면·결과 Dashboard 연결

- **완료:** PR #11 병합 확인 후 최신 dev에서 판정 API를 화면에 연결. `YPC_API_BASE`가 설정되면 분석 화면에서 저장된 BE 프로필을 조회해 판정을 요청하고, 성공 시 실제 verdict·confidence·조건 근거를 Dashboard와 정책 상세에 표시. API 미설정은 Mock 결과 fallback, 프로필 없음·실패·로딩은 화면 상태로 안내.
- **검증:** `npm.cmd test` 69개, `npm.cmd run check`, `git diff --check` 통과. live Dashboard 3분류 요약, 조건 4상태·미래 날짜·영구 불충족·담당부서 근거·HTML escape, empty/error 상태와 정적 서버 모듈 제공을 검증.
- **다음:** 실제 BE 주소/CORS 브라우저 E2E, 분석 재요청·새로고침 정책, 질문·조합·서류 화면 연결. M1 소득·동의 계약 및 M2 전체 완료 조건은 남아 있음.

## 2026-09-19 · M2 판정 API 클라이언트와 응답 검증

- **완료:** PR #10 병합 확인 후 최신 dev에서 독립 작업. `POST /v1/judge?include=all` 클라이언트, 15초 제한 시간·요청 취소·오류 구분 구현. BE의 3개 정책 verdict와 별도 confidence를 보존하는 응답 검증기 및 조건 4상태 변환 함수 추가. 전체 목록/요약 일치, 중복 조건, 원문 근거, 개별 날짜, 담당부서 연락처 검증.
- **검증:** `npm.cmd test` 66개 및 `npm.cmd run check`, `git diff --check` 통과. 가상 계약 fixture와 임시 로컬 HTTP 서버 사용. 실제 BE/CORS/브라우저 E2E는 미수행.
- **다음:** 저장된 서버 프로필 → 명시적 분석 요청 → Loading/Error/Empty → 실제 결과·상세 화면 연결. M1의 소득·동의 및 실제 BE 연결 조건도 남아 있음.
- **범위:** [판정 API 문서](Judgement-api.md). 앱 화면에는 아직 호출을 연결하지 않아 기존 Mock 표시 유지. 정책 전체 미래 가능일·4분류는 합의 전이며 FE가 계산하지 않음. M2 전체 완료가 아님.

## 2026-09-19 · M1 지역 코드 오매핑 수정

- **완료:** PR #9 병합 확인 후 최신 dev에서 작업. “용인시”를 수지구 `41465`로, “그 외 지역”을 전국 `00`으로 치환하던 매핑 제거. 행정표준코드 조회로 용인시 처인구 `41461`·기흥구 `41463`·수지구 `41465`를 대조하고 서버 선택지와 API 허용 목록을 단일 정의로 통합. 모호한 지역의 신규 저장 차단, 기존 코드 유지 및 상세 지역 확인 안내.
- **검증:** `npm.cmd test` 57개, `npm.cmd run check`, `git diff --check` 통과. 3개 구의 왕복 직렬화, 구 초안/범위 코드 신규 전송 차단, 기존 목록 밖 코드와 숨겨진 데이터 보존, 명시적 지역 수정, 폼 표시 검증. 실제 브라우저/BE E2E는 미수행.
- **다음:** 지원 지역 확장 및 기존 지역 확인, 소득·근로 형태·동의 수집 계약 확정, 실제 BE Base URL/CORS를 통한 연결 검증. 정책 판정 API 연결과 M1 전체 완료는 아직 남아 있음.

## 2026-09-19 · M1 서버 입력 계약 반영

- **완료:** GitHub Issue #2와 BE `dev`의 `app/schemas/user.py`, `app/schemas/enums.py`를 확인. 서버 모드에 학력 6종·취업 5종·혼인 4종 선택지 반영. 거주 시작일 미입력 허용, 제공된 날짜 검증 유지. 미지원 소득·근로 형태·정책 참여 이력 변경은 요청 전 필드별 오류 및 포커스 링크로 안내. 하단 진행 표를 최신 구현에 맞게 정정.
- **검증:** `npm.cmd test` 53개, `npm.cmd run check` 통과. 선택지 전송, 거주 시작일 null, 기존 history/answers/consent 보존, 미지원 입력의 서버 요청 차단과 폼 오류 이동 검증. 폼 테스트는 DOM 대역이며 실제 브라우저 E2E는 아님.
- **다음:** 지역 선택지·코드, 소득 입력 방식·근로 형태 수집 여부 및 동의 수집 확정. 테스트 Base URL 확보 후 실제 BE/CORS·브라우저 검증으로 M1 완료 판단.
- **남은 조건:** 이번 변경은 M1 일부 구현이며 실제 정책 조회·판정 연결은 여전히 Mock. M2 이후 완료를 뜻하지 않음.

## 2026-09-17 · PR #8 재마운트 리뷰 반영

- **완료:** 프로필 화면의 빠른 재진입이 진행 중인 조회를 공유하도록 수정. 저장·삭제 중 재진입은 요청 종료 후 새로 조회. 실제 조회 실패는 오류 및 재시도 UI로 전달.
- **검증:** 실제 mountProfile 함수를 DOM 대역으로 실행하는 재마운트·실제 실패/재시도 테스트와 저장·삭제 대기 테스트 추가. 전체 50개 테스트 및 문법 검사 통과. 실제 브라우저 검증을 대체하지 않음.

## 2026-09-17 · 프로필 폼 서버 연결

- **완료:** PR #7 병합 확인. `YPC_API_BASE` 설정 시 두 프로필 진입점에서 서버 조회·생성·수정·삭제 사용. 조회 실패 재시도, 중복 요청 차단, 저장 실패 시 입력 보존, 삭제 확인 및 재시도 구현.
- **데이터 보존:** 전체 교체 PUT에 대비해 폼에서 변경한 필드만 갱신하고 기존 core 추가 필드·history·answers·consent 보존. 편집 불가능한 기존 enum/지역 값은 유지 옵션으로 표시. 미지원 입력은 전송 전 차단.
- **검증:** 단위 테스트 및 임시 로컬 HTTP 서버를 통한 생성→조회→수정→삭제 검증. 연결 가능한 브라우저와 실제 BE 주소가 없어 브라우저/CORS/운영 서버 E2E는 미수행.
- **다음:** 소득·선택지·지역 코드 계약과 동의 수집 확정, 실제 BE 연결 검증 후 M1 완료 판정. 정책 판정 API는 아직 Mock 상태.

## 2026-09-16 · Profile API 오류 처리 보강

- **완료:** 삭제 실패 시 세션 ID를 보존해 재시도 가능하도록 수정. 삭제 404는 이미 삭제된 상태로 처리. 조회 응답의 profile 누락·잘못된 타입·손상된 JSON을 오류로 구분하고 본문 수신 실패를 네트워크 오류로 정규화.
- **검증:** `npm.cmd test` 36개 통과, `npm.cmd run check` 통과. 실제 BE 통합 및 브라우저 검증은 수행하지 않음.
- **다음:** 폼의 미지원 입력값과 BE 필드 매핑을 확정한 뒤 서버 조회·저장·삭제 UI 연결. PUT은 전체 교체이므로 기존 answers/history/consent 및 폼에 없는 core 필드를 보존해야 함.
- **남은 조건:** 실제 API 주소 및 미지원 필드 계약 확정. M1 전체 완료는 서버 연결 E2E 검증 후 판단.

## 2026-09-13 · PR #3 문서 리뷰 반영

- **완료:** 조건 4개 상태/3개 응답 배열의 불일치와 askable 정의 누락을 PRD·현재 detail/questions 코드와 대조하고, 합의 질문·예시 응답 확인 기준을 계약 문서에 추가. 문서 범위를 MVP 전체 인터페이스로 명시하고 브랜치 규칙을 FE 저장소에 한정.
- **다음:** Issue #2에서 조건 결과 컨테이너·askable 규칙 확인 후 별도 기능 PR에서 DTO/화면 반영.
- **막힌 점:** BE·AI의 실제 응답 예시와 필드 정의 미확정.
- **결정 필요:** FUTURE_PASS 조건의 배열 위치·개별 날짜, askable 의미·타입·생성 주체·질문 노출 책임.

## 2026-09-13 · Final MVP 기준 재정리

- **완료:** 수정 PRD·Milestone 반영 및 기존 Mock과 새 정책 verdict·신뢰도·근거·Profile 필드 차이 문서화.
- **다음:** 독립 브랜치에서 M1 입력·수정·클라이언트 검증 구현. API 연동은 별도 PR.
- **막힌 점:** Issue #2의 Profile API·JudgementResult 합의 및 실제 서버 정보 미확정.
- **결정 필요:** 3개 정책 verdict에서 4개 화면 분류로의 표현 규칙, enum·소득 기간·필수값, op/operator 및 sourceQuote/source_quote 표기.

새 Milestone에 맞춰 독립적으로 구현할 수 있는 입력 UI/검증을 진행한다. 아래 이전 기록의 일괄 보류 문구는 당시 기준이며, 실제 완료 체크는 여전히 통합 검증 후에만 한다.

## 2026-09-13 · PR #1 리뷰 반영

- **완료:** 질문·서류·일정·조합·충돌 정책 참조와 화면 데이터 형태 검증 추가. 실제 달력 날짜 검증 추가. 정적 서버 공개 파일을 명시적 허용 목록으로 제한. 계획서 추출 텍스트 정리. 컬러 변경은 기존 프로토타입에 적용됐음을 PR에 명시.
- **다음:** BE/AI 계약 확인 후 M1 입력 검증·Profile 연동.
- **막힌 점:** 실제 API와 브라우저 UI 검증은 여전히 미완료.
- **결정 필요:** 기존 M0 계약 합의 항목 유지.

## 2026-09-12

- **완료:** M0 화면 경로 7개, 공통 상태·카드·필터·정책별 상세를 Mock JSON에 연결. 원본 초안 보존. 계약 테스트 3개 및 문법 검사 통과. 개발 서버와 Mock JSON HTTP 200 확인.
- **다음:** BE/AI와 `Frontend-contract.md` 및 JSON 계약 확정. M0 통과 후 M1 사용자 입력 Validation 및 Profile 저장/조회 연결.
- **막힌 점:** BE API/확정 DTO/AI 결과 없음. 브라우저 연결이 없어 실제 렌더링, 키보드 이동, 반응형 검증은 미완료.
- **결정 필요:** API 계약·인증·분석 작업 상태·질문 DTO 확정 및 FE 와이어프레임 검토.

## 순서별 진행 상태

| 단계 | 상태 | 다음 통과 조건 |
| --- | --- | --- |
| M0 | FE 초안 구현 및 데이터 테스트 완료, 팀 계약 합의 대기 | 브라우저 UI 확인, BE/AI 동일 JSON 검증 |
| M1 | 입력·수정·검증 및 프로필 서버 CRUD 연결 구현, 실제 BE 통합 검증 대기 | 지역·소득·동의 계약 확정, 실제 서버 저장·정책 조회 및 브라우저 E2E |
| M2 | 판정 API 클라이언트·검증기·조건 상태 변환 구현, 화면은 Mock | 실제 판정 요청·결과·상세 화면 연결 및 E2E |
| M3 | 미착수 | 답변 후 실제 재판정 및 조합 생성 |
| M4 | 미착수 | 실제 서류·일정 API 포함 전체 통합 |
| M5 | 미착수 | 모바일/데스크톱, 6개 E2E, 제출 패키지 |

M1 구현은 진행 중이며 서버 연결 코드와 모의 서버 테스트만으로 전체 완료를 체크하지 않는다. 와이어프레임 존재만으로 M2~M4 기능 완료를 체크하지 않는다.
